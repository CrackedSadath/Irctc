 name: Publish 🚀

 on:
  workflow_dispatch:
  push:
    branches:
    - main

 jobs:
   publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: setup .NET
        uses: actions/setup-dotnet@v3
        with:
           dotnet-version: '6.0.X'

    

      - name: Build
        run: dotnet build ./C:\Users\91855\Source\Repos\CrackedSadath\Irctc\pom.xml --configuration Release --no-restore

      - name: Publish
        run: dotnet publish ./C:\Users\91855\Source\Repos\CrackedSadath\Irctc\pom.xml  --configuration Release --no-build
